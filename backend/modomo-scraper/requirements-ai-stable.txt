# Modomo Dataset Scraper - Stable AI Requirements
# Conservative versions that work reliably together

# Core API Framework (inherit from minimal)
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
asyncpg>=0.28.0
redis>=4.0.0
boto3>=1.20.0
pillow>=9.0.0
numpy>=1.21.0,<1.25.0
requests>=2.25.0
aiohttp>=3.8.0
supabase>=2.0.0
playwright>=1.30.0
beautifulsoup4>=4.10.0
python-multipart>=0.0.5
aiofiles>=22.0.0
python-dotenv>=0.19.0

# ============================================================================
# AI/ML DEPENDENCIES - STABLE VERSIONS
# ============================================================================

# PyTorch CPU (Railway compatible - no CUDA dependencies)
torch==2.0.1
torchvision==0.15.2
torchaudio==2.0.2

# Transformers ecosystem (stable versions)
transformers==4.33.3
tokenizers==0.13.3
huggingface_hub==0.15.1
timm>=0.6.0

# Basic ML utilities (avoid version conflicts)
scikit-learn==1.3.0
scipy==1.11.3

# Image processing (stable)
opencv-python-headless==4.8.0.76

# Logging
structlog==23.1.0
loguru==0.7.2

# Background processing
celery==5.3.4

# Embeddings (simpler approach)
sentence-transformers==2.2.2

# Color extraction
webcolors==1.13

# Performance
psutil>=5.9.0