# Minimal requirements for Celery-only deployment
# Use this when you only need background task processing without heavy AI dependencies

# Core FastAPI
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0

# Celery and Redis
celery[redis]>=5.3.0,<6.0.0
redis>=4.0.0,<6.0.0
flower>=2.0.0

# Database
supabase>=2.0.0,<3.0.0
websockets>=10.0,<12.0
asyncpg>=0.28.0

# Basic utilities
structlog>=22.0.0
python-dotenv>=1.0.0
aiofiles>=23.0.0
python-multipart>=0.0.6

# Performance monitoring
psutil>=5.9.0

# Minimal AI (only if needed for classification)
# torch>=2.0.0,<2.2.0  # Uncomment only if AI features needed
# transformers>=4.30.0,<4.36.0  # Uncomment only if AI features needed
# Pillow>=9.0.0  # Uncomment only if image processing needed

# Optional: datasets for HuggingFace imports (may cause gradio conflicts)
# datasets>=2.14.0,<2.19.0  # Comment out if gradio conflicts persist