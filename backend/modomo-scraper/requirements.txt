# Minimal requirements for Heroku Celery worker (import/scraping only)
# Core framework
fastapi>=0.100.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
structlog>=23.0.0

# Database & Redis
redis>=5.0.0
supabase>=2.0.0
websockets>=10.0,<12.0

# Storage
boto3>=1.34.0

# Celery for background tasks
celery[redis]>=5.3.0

# Web scraping (for import tasks)
requests>=2.31.0
beautifulsoup4>=4.12.0

# HuggingFace datasets for import tasks
datasets>=2.14.0

# Avoid problematic packages
# playwright  # Too heavy for worker
# asyncpg     # Not needed for worker